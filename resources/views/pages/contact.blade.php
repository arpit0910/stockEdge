@extends('layouts.app')
@section('title',$pageContent->title)
@section('content')
@include('components.page-heading')
<section class="container section compact contact-layout"><div>@include('components.page-body')<div class="contact-details">@if(config('stockedge.site.contact_email'))<p><b>Email</b><br><a href="mailto:{{ config('stockedge.site.contact_email') }}">{{ config('stockedge.site.contact_email') }}</a></p>@endif @if(config('stockedge.site.contact_phone'))<p><b>Phone</b><br>{{ config('stockedge.site.contact_phone') }}</p>@endif @if(config('stockedge.site.contact_address'))<p><b>Address</b><br>{{ config('stockedge.site.contact_address') }}</p>@endif</div></div><div class="form-panel"><form method="post" action="{{ route('lead') }}">@csrf<input type="hidden" name="type" value="contact"><label>Your name<input name="name" value="{{ old('name') }}" required maxlength="120"></label><label>Email address<input type="email" name="email" value="{{ old('email') }}" required></label><label>Phone number (optional)<input type="tel" name="phone" value="{{ old('phone') }}" maxlength="30"></label><label>How can we help?<textarea name="message" rows="5" required maxlength="5000">{{ old('message') }}</textarea></label><label class="consent"><input type="checkbox" name="consent" value="1" required> I consent to my details being used to respond to this enquiry, as described in the <a href="{{ route('page','privacy') }}">privacy policy</a>.</label><button class="button full">Send your message ↗</button></form></div></section>
@endsection