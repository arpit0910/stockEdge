const menu=document.querySelector('.a-menu-button');const sidebar=document.getElementById('admin-sidebar');
menu?.addEventListener('click',()=>{const isOpen=sidebar.classList.toggle('open');menu.setAttribute('aria-expanded',String(isOpen));});
document.addEventListener('click',e=>{if(sidebar?.classList.contains('open')&&!sidebar.contains(e.target)&&!menu.contains(e.target)){sidebar.classList.remove('open');menu.setAttribute('aria-expanded','false');}});
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&sidebar?.classList.contains('open')){sidebar.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.focus();}});
document.querySelectorAll('[data-dismiss]').forEach(b=>b.addEventListener('click',()=>b.closest('.a-flash').remove()));
const dirtyForms=document.querySelectorAll('[data-dirty-form]');let dirty=false;dirtyForms.forEach(form=>{form.addEventListener('input',()=>{dirty=true;document.querySelectorAll('[data-save-state]').forEach(el=>{el.textContent='Unsaved changes';el.classList.add('unsaved');});});form.addEventListener('submit',()=>{dirty=false;});});window.addEventListener('beforeunload',e=>{if(dirty){e.preventDefault();e.returnValue='';}});
document.querySelectorAll('[data-character-count]').forEach(input=>{const counter=input.nextElementSibling;const update=()=>{counter.textContent=input.value.length.toLocaleString()+' characters';};input.addEventListener('input',update);update();});
const title=document.querySelector('[data-title-field]');const slug=document.querySelector('[data-slug-field]');let autoSlug=slug&&!slug.value;slug?.addEventListener('input',()=>autoSlug=false);title?.addEventListener('input',()=>{if(autoSlug){slug.value=title.value.normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');}});
const confirmDialog=document.getElementById('confirm-dialog');let deleteForm=null;document.querySelectorAll('[data-delete-form]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();deleteForm=form;confirmDialog.showModal();}));confirmDialog?.addEventListener('close',()=>{if(confirmDialog.returnValue==='confirm'&&deleteForm){dirty=false;HTMLFormElement.prototype.submit.call(deleteForm);}deleteForm=null;});
document.querySelector('[data-preview-button]')?.addEventListener('click',()=>{const dialog=document.getElementById('body-preview');const target=dialog.querySelector('[data-preview-content]');target.replaceChildren();const value=document.getElementById('field-body')?.value||'';value.split(/\r?\n\r?\n/).filter(Boolean).forEach(section=>{const lines=section.split(/\r?\n/);const heading=document.createElement('h2');heading.textContent=lines.shift();target.append(heading);if(lines.length){const p=document.createElement('p');p.textContent=lines.join(' ');target.append(p);}});if(!value){const p=document.createElement('p');p.textContent='Add body text to preview its structure.';target.append(p);}dialog.showModal();});
